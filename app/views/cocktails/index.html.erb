  <div class="#"></div>

  <div id='thing' class="card" style='margin-top: -100px;'>
    <h1 id='list-head'>  Cocktails  🍸 </h1>
  </div>

<div class="container">
  <h1>Cocktails (<%= @cocktails.length %>)</h1>
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <div class="col-xs-12 col-md-3">
        <div class="card">
          <%= cl_image_tag cocktail.photo, height: 150, width: 150, crop: :thumb %>
          <p>
          <%=link_to cocktail.name, cocktail_path(cocktail) %> <%= link_to '#', cocktail_path(cocktail),  method: :delete, class: 'cocktail-link' %>
          </p>
        </div>
      </div>
    <% end %>
  </div>

  <br>
  <%= link_to 'Add your own cocktail', new_cocktail_path, class: 'btn btn-primary' %>
</div>
